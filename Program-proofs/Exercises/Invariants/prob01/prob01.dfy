/* file: prob01.dfy
   author: your name
   description: extra practice in Dafny, invariants, prob01
   This is exercise 7.1 from the PC reader
*/

method problem01(n:nat) returns (x:int, y:int)
ensures x == n * n && y == n * n * n
{
  // Design a command S such that {P} S {Q} 
  // Make use of the heuristic which replaces 
  // a constant (here n) with a variable (k) 
  // to find J and B
  
}